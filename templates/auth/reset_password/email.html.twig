<h1>Hi!</h1>

<p>{{ 'To reset your password, please visit the following link'|trans }}</p>

<a href="{{ url('syncengine_reset_password_reset', {token: resetToken.token}) }}">{{ url('syncengine_reset_password_reset', {token: resetToken.token}) }}</a>

<p>{{ 'This link will expire in {expiration}.'|trans({'expiration': resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle')}) }}</p>

<p>{{ 'Cheers!'|trans }}</p>
