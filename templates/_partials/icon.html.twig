{% set icon = icons[ icon ] ?? icon %}

{% if icon starts with '<svg' %}
	<i class="icon {{ class ?? '' }}">{{ icon|raw }}</i>
{% else %}
	<i class="icon {{ icon }} {{ class ?? '' }}"></i>
{% endif %}
