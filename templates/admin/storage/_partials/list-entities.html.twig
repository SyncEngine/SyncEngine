{% set header = header ?? {
	icon: 'bi-database',
	title: 'Storages'|trans,
	actions: [ 'create' ],
} %}

{% set columns = columns ?? {
	entities: {
		label: 'Used in'|trans,
		prop: '_dependents',
	},
	config: {
		label: "Columns",
		prop: 'columns',
	},
} %}

{% include 'admin/_partials/list-entities.html.twig' with {
	type: 'storage',
	entity: true,
} %}
