{% set header = header ?? {
	icon: 'storage',
	title: 'Storages'|trans,
	actions: [ 'create' ],
} %}

{% set columns = columns ?? {
	info: {
		label: 'Info',
		badge: {
			label: '{{ type }}',
			icon: 'storage-{{ type }}',
		},
	},
	entities: {
		label: 'Used in'|trans,
		prop: '_dependents',
	},
	config: {
		label: "Columns",
		prop: 'columns',
		inline: true,
	},
} %}

{% include 'admin/_partials/list-entities.html.twig' with {
	type: 'storage',
	entity: true,
} %}
