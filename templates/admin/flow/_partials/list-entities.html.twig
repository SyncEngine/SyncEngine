{% set header = header ?? {
	icon: 'flow',
	title: 'Flows'|trans,
	actions: [ 'create' ],
} %}

{% set columns = columns ?? {
	relations: {
		block: 'entities',
		label: 'Used in'|trans,
		prop: '_dependents',
	},
	steps: {
		block: 'entities',
		label: 'Steps'|trans,
		prop: '_dependencies',
	},
} %}

{% include 'admin/_partials/list-entities.html.twig' with {
	type: 'flow',
	entity: true,
} %}
