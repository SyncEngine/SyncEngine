{% extends 'admin/index.html.twig' %}

{% block title %}{{ 'Connections'|trans({},'core') }}{% endblock %}

{% block content %}

	{% include 'admin/_partials/list.html.twig' with {
		type: 'connection',
		entity: true,
		header: {
			icon: 'bi-cloud',
			title: 'Connections'|trans({},'core'),
			actions: [ 'create' ],
		},
		query: { limit: 10, total: true, relations: true, dependents: true },
		columns: {
			entities: {
				label: 'Used in'|trans({},'core'),
				prop: '_dependents',
			},
			config: {
				label: 'Webservice'|trans({},'core'),
				type: 'webservice',
				typeLabel: 'Webservice'|trans({},'core'),
				prop: 'webservice',
				multi: false,
				itemLabelProp: 'host',
			},
		}
	} %}

{% endblock %}
