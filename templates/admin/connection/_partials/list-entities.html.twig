{% set header = header ?? {
	icon: 'connection',
	title: 'Connections'|trans,
	actions: [ 'create' ],
} %}

{% set columns = columns ?? {
	info: {
		label: 'Info'|trans,
		badge: {
			options: [
				{
					compare: false,
					value: '{{ data.connected }}',
					label: 'Issue'|trans,
					type: 'warning',
					icon: 'authorization-error',
				},
				{
					compare: true,
					value: '{{ data.connected }}',
					label: 'Connected'|trans,
					type: 'success',
					icon: 'webhook',
				}
			]
		},
	},
	entities: {
		label: 'Used in'|trans,
		prop: '_dependents',
	},
	webservice: {
		block: 'info',
		label: 'Webservice'|trans,
		type: 'webservice',
		value: '{{ config.webservice._class }}',
		parse: 'tag|model',
		sub: {
			value: '{{ config.webservice.host }}',
			parse: 'tag',
		}
	},
} %}

{% include 'admin/_partials/list-entities.html.twig' with {
	type: 'connection',
	entity: true,
} %}
